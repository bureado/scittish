name: Integration Tests

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]
  workflow_dispatch:  # Allow manual trigger

jobs:
  integration:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install oras CLI
      run: |
        VERSION="1.1.0"
        curl -LO "https://github.com/oras-project/oras/releases/download/v${VERSION}/oras_${VERSION}_linux_amd64.tar.gz"
        mkdir -p oras-install/
        tar -zxf oras_${VERSION}_*.tar.gz -C oras-install/
        sudo mv oras-install/oras /usr/local/bin/
        rm -rf oras_${VERSION}_*.tar.gz oras-install/
        oras version
    
    - name: Checkout SCITT CCF Ledger
      uses: actions/checkout@v4
      with:
        repository: microsoft/scitt-ccf-ledger
        path: scitt-ccf-ledger
    
    - name: Build SCITT ledger
      working-directory: scitt-ccf-ledger
      run: |
        export PLATFORM=virtual
        ./docker/build.sh
    
    - name: Start SCITT ledger
      working-directory: scitt-ccf-ledger
      run: |
        export PLATFORM=virtual
        # The run-dev.sh script typically ends with "docker logs -f" which blocks
        # We need to modify it to not follow logs, or manually start the container
        # Let's modify the script to remove the blocking logs command
        sed -i 's/docker logs -f/docker logs/' ./docker/run-dev.sh || true
        sed -i '/docker logs/d' ./docker/run-dev.sh || true
        
        # Now run the script
        ./docker/run-dev.sh
        
        # Wait for ledger to be ready (retry for up to 120 seconds)
        echo "Waiting for SCITT ledger to be ready..."
        for i in {1..120}; do
          if curl -s http://localhost:8000 >/dev/null 2>&1; then
            echo "SCITT ledger is ready!"
            break
          fi
          echo "Attempt $i: SCITT ledger not ready yet, waiting..."
          sleep 1
        done
        
        # Verify it's running
        curl -v http://localhost:8000 || true
    
    - name: Start OCI registry
      run: |
        docker run -d -p 5000:5000 --name registry registry:2
        
        # Wait for registry to be ready
        echo "Waiting for OCI registry to be ready..."
        for i in {1..30}; do
          if curl -s http://localhost:5000/v2/ >/dev/null 2>&1; then
            echo "OCI registry is ready!"
            break
          fi
          echo "Attempt $i: OCI registry not ready yet, waiting..."
          sleep 1
        done
        
        # Verify it's running
        curl -v http://localhost:5000/v2/
    
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r api/requirements.txt -r requirements-dev.txt
    
    - name: Run integration tests
      run: |
        pytest -m integration -v
    
    - name: Show ledger logs on failure
      if: failure()
      working-directory: scitt-ccf-ledger
      run: |
        docker ps -a
        docker logs $(docker ps -a --filter "name=ccf" --format "{{.Names}}" | head -n 1) || true
    
    - name: Show registry logs on failure
      if: failure()
      run: |
        docker logs registry || true
